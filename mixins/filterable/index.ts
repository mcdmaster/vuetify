// @ts-nocheck
/* eslint-disable */

import Vue from 'vue'

/* @vue/component */
export default options
const options: ThisType<typeof Vue> = {
  name: 'filterable',

  props: {
    noDataText: {
      type: String,
      default: '$vuetify.noDataText',
    },
  },
}
