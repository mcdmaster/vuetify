// @ts-nocheck
/* eslint-disable */

import Vue from 'vue'

export default options
const options: ThisType<typeof Vue> = {
  name: 'transitionable',

  props: {
    mode: String,
    origin: String,
    transition: String,
  },
}
